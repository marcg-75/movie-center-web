const colors = require('./colors.js');
const spacing = require('./spacing.js');
const textScale = require('./textScale.js');
const opacity = require('./opacity.js');
const shadow = require('./shadow.js');
const borderRadius = require('./borderRadius.js');

module.exports = { colors, spacing, textScale, opacity, shadow, borderRadius };
